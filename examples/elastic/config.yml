# examples/elastic/config.yml
# EXAMPLE CONFIGURATION - Replace with your actual values

output.elasticsearch:
  hosts: ["http://192.0.2.10:9200"]  # TEST-NET-1 (RFC 5737)
  # OR use localhost for real deployments
  # hosts: ["http://localhost:9200"]

setup.kibana:
  host: "http://192.0.2.10:5601"  # TEST-NET-1 (RFC 5737)
  # OR use localhost for real deployments  
  # host: "http://localhost:5601"
